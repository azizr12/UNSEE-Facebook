# Unseen for Facebook

A small Chrome extension that prevents Facebook story view tracking.

## Features

- Prevents the Facebook story seen-state mutation from being sent.
- Works on Facebook and Messenger web pages.
- One simple setting in the popup.
- No icons, notifications, analytics, or external update service.
- Settings are stored locally in Chrome.

## Installation

1. Open `chrome://extensions`.
2. Enable **Developer mode**.
3. Choose **Load unpacked**.
4. Select this repository directory.

## Updates

When distributed through the Chrome Web Store, Chrome handles extension updates automatically. The extension deliberately does not implement a custom GitHub updater because Chrome does not allow a normal extension to silently replace its own installed package.
